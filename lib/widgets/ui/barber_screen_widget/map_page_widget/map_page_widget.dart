import 'package:flutter/material.dart';

class BarberMapPage extends StatelessWidget {
  const BarberMapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Map page'),
    );
  }
}